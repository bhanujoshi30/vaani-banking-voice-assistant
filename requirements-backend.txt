# Backend API Dependencies Only
# Minimal requirements for backend deployment on Vercel

# Core FastAPI and server
fastapi>=0.115.0
uvicorn[standard]>=0.31.0
starlette>=0.27.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Data validation and serialization
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Authentication and security
python-jose[cryptography]>=3.3.0
passlib>=1.7.4
bcrypt>=4.0.0

# HTTP and form handling
python-multipart>=0.0.12
httpx==0.27.2

# Voice verification (required for voice login)
resemblyzer>=0.1.2
librosa>=0.10.0
soundfile>=0.12.0
numpy>=1.24.0
scipy>=1.10.0

# Utilities
python-dotenv==1.0.1
tenacity==8.5.0

# Logging (optional, but good to have)
structlog==24.4.0

# Testing (optional, but included for completeness)
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# Fake data for seeding (optional)
faker>=19.0

